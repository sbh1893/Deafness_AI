# Deafness_AI — 소프트웨어 사전 프로토타입 위키

> 청각장애인을 위한 AI 기반 자가 조음 훈련 시스템, 1단계 MVP 기획 보고서의
> "0-A단계: 소프트웨어 사전 프로토타입"에 해당하는 코드입니다.
> sEMG 센서 배송을 기다리는 동안, 하드웨어 없이 지금 바로 실행해서
> 웹캠·마이크 단독 성능(baseline)을 먼저 확인하기 위한 목적입니다.

---

## 1. 이게 왜 필요한가 (배경)

보고서 8장에서는 본격 개발 전에 파일럿 테스트로 sEMG·VSR·음향 3개 채널의
실제 판별 가능 범위를 먼저 확인하기로 했습니다. 이 중 **sEMG 센서는 배송을
기다려야 하지만, 웹캠(VSR)과 마이크(음향)는 지금 바로 테스트할 수 있습니다.**

이 폴더의 코드는 그 두 채널만 먼저 돌려보고:
- 웹캠 입모양만으로 목표 음소가 어느 정도 구분되는지
- 마이크 음향만으로 목표 음소가 어느 정도 구분되는지

를 숫자(baseline 정확도)로 미리 확인해두는 것이 목표입니다. 나중에
sEMG가 도착해서 融합(3채널 결합) 성능을 측정할 때, "sEMG를 추가했더니
정말 좋아졌는가"를 비교할 기준선이 됩니다.

---

## 2. 폴더 구조

```
Deafness_AI/
├── WIKI.md                          <- 이 문서
├── 01_webcam_vsr/
│   ├── lip_landmark_capture.py      <- 웹캠 입모양 테스트 코드
│   ├── requirements.txt
│   └── lip_features.csv             <- 실행 후 자동 생성되는 결과 데이터
├── 02_mic_acoustic/
│   ├── audio_feature_capture.py     <- 마이크 음향 테스트 코드
│   ├── requirements.txt
│   └── audio_features.csv           <- 실행 후 자동 생성되는 결과 데이터
└── 03_semg/
    ├── arduino_emg_reader/
    │   └── arduino_emg_reader.ino   <- 아두이노 EMG 수집 스케치
    ├── semg_feature_capture.py      <- sEMG 신호처리·분석 파이썬 스크립트
    ├── requirements.txt
    └── semg_features.csv            <- 실행 후 자동 생성되는 결과 데이터
```

---

## 3. 두 코드가 각각 어떻게 작동하는가 (LLM 위키 스타일 설명)

### 3-1. `01_webcam_vsr/lip_landmark_capture.py` — 웹캠 입모양(VSR) 테스트

**한 줄 요약:** 웹캠으로 얼굴을 찍고, 입 주변 468개 점(랜드마크) 중 4개
핵심 지점만 뽑아서 "입이 얼마나 벌어졌는지 / 얼마나 넓게 벌어졌는지"를
숫자로 바꾼 다음, 그 숫자만으로 음소를 구분할 수 있는지 테스트합니다.

**사용하는 기술:**
| 구성 요소 | 역할 |
|---|---|
| OpenCV (`cv2`) | 웹캠에서 영상을 프레임 단위로 읽어옴 |
| MediaPipe FaceMesh | 얼굴에서 468개 랜드마크(점) 좌표를 실시간으로 추출하는 구글의 사전학습 모델 |
| NumPy | 랜드마크 좌표 간 거리 계산 |
| scikit-learn (RandomForest) | 모은 데이터로 "이 특징만으로 라벨을 맞힐 수 있는가"를 검증하는 간단한 분류기 |

**작동 순서(파이프라인):**
1. 웹캠에서 프레임을 계속 읽어옵니다.
2. 각 프레임을 MediaPipe FaceMesh에 넣어 얼굴 랜드마크 좌표를 얻습니다.
3. 그중 입술 위/아래(13, 14번), 입꼬리 좌/우(61, 291번) 4개 점만 사용해서:
   - `mouth_width` = 입꼬리 좌우 거리
   - `mouth_height` = 입술 위아래 거리
   - `aspect_ratio` = height / width (입이 세로로 길게 벌어졌는지, 가로로 넓게 벌어졌는지)
   를 계산합니다. (얼굴 좌우 폭으로 나눠서, 카메라와의 거리 차이에 따른 오차를 보정합니다.)
4. 사용자가 콘솔에 라벨(m, n, ng, g, k, kk)을 입력하고 그 음소를 반복 발음하면,
   1.5초 동안의 프레임들이 해당 라벨과 함께 `lip_features.csv`에 저장됩니다.
5. `q`를 입력해 종료하면, 모아진 데이터로 RandomForest라는 간단한 분류
   모델을 학습시켜 교차검증 정확도를 출력합니다. 이 정확도가
   "웹캠 입모양만으로 이 음소들을 얼마나 구분할 수 있는가"의 baseline입니다.

**왜 이렇게 단순하게 만들었나 (설계 근거):**
이 코드는 정식 CNN 기반 VSR 모델이 아니라, 기하학적 특징(너비/높이/비율)
3개만 쓰는 매우 단순한 버전입니다. 보고서 4~5장에서 이미 "웹캠은 눈에
보이는 입술 모양은 잘 잡지만, 혀 뒤쪽에서 조음되는 연구개음(ㄱ,ㅋ,ㄲ)은
입모양만으로 판별하기 어렵다"는 문헌 근거를 정리했습니다. 이 코드는 그
가설을 실제 자기 발음으로 빠르게 검증해보기 위한 것이므로, 처음부터
무거운 딥러닝 모델을 붙이기보다 가장 단순한 특징으로 먼저 확인하는
것이 합리적입니다.

---

### 3-2. `02_mic_acoustic/audio_feature_capture.py` — 마이크 음향 테스트

**한 줄 요약:** 마이크로 발음을 녹음하고, 소리의 높낮이(피치)와 음색
(스펙트럴 특징)을 숫자로 바꾼 다음, 그 숫자만으로 음소를 구분할 수
있는지 테스트합니다.

**사용하는 기술:**
| 구성 요소 | 역할 |
|---|---|
| sounddevice | 마이크에서 오디오를 녹음 |
| librosa | 오디오 신호에서 피치(F0), 스펙트럴 특징, MFCC를 추출하는 음성처리 라이브러리 |
| scikit-learn (RandomForest) | 웹캠 코드와 동일하게 baseline 분류 정확도 확인 |

**작동 순서(파이프라인):**
1. 사용자가 라벨을 입력하면 1.5초간 마이크로 녹음합니다.
2. librosa로 다음 특징을 뽑습니다:
   - `mean_pitch`: `librosa.pyin`으로 추정한 평균 피치(음높이, Hz)
   - `spectral_centroid` / `spectral_bandwidth`: 소리의 "밝기"와 "퍼짐 정도"를
     나타내는 지표로, 비음(콧소리)처럼 공명 특성이 달라지는 소리를
     간접적으로 반영합니다.
   - `mfcc1~mfcc13`: 음색을 요약하는 13개의 표준 음성 특징값(MFCC)
3. 라벨과 함께 `audio_features.csv`에 저장합니다.
4. `q` 입력 시, 웹캠 코드와 동일하게 RandomForest로 baseline 정확도를 확인합니다.

**왜 이렇게 만들었나 (설계 근거):**
보고서 6장에서 "비음은 음향학적으로 뚜렷한 특징을 남기므로, 별도의
피에조 비강 센서 없이도 마이크만으로 상당 부분 탐지될 가능성이 있다"고
정리했습니다. 이 코드는 정밀한 포먼트(F1/F2/F3) 분석까지는 하지 않고
`spectral_centroid`/`spectral_bandwidth`라는 손쉬운 근사 지표로 이
가설을 먼저 빠르게 확인해보는 용도입니다. (더 정밀한 포먼트 분석이
필요하면 추후 `parselmouth`(Praat 파이썬 래퍼) 도입을 2단계에서 검토하세요.)

---

### 3-3. `03_semg/` — sEMG(근전도) 신호처리 & 분석

**한 줄 요약:** 아두이노가 1000Hz로 근전도 원시 신호를 계속 읽어서 시리얼로 보내고, 파이썬이 그걸 받아 실전 수준의 필터링(대역통과+노치+정류+포락선)을 거친 뒤, 개인별 최대 힘(MVC) 대비로 정규화하고, 세션을 분리한 상태로 baseline 정확도를 확인합니다.

**구성 파일:**
- `arduino_emg_reader/arduino_emg_reader.ino` — 센서 원시값을 읽어 PC로 전송하는 아두이노 스케치
- `semg_feature_capture.py` — 신호처리·정규화·분류기 검증을 담당하는 파이썬 스크립트

**사용하는 기술:**
| 구성 요소 | 역할 |
|---|---|
| 아두이노 `micros()` 타이밍 | 1000Hz(1ms 간격) 샘플링을 최대한 일정하게 유지 |
| pyserial | 아두이노-파이썬 간 시리얼 통신 |
| scipy.signal (butter, filtfilt, iirnotch) | 대역통과(20~450Hz)·노치(60Hz) 필터, 포락선 추출용 저역통과 필터 |
| numpy | 정류, RMS 등 특징 계산 |
| scikit-learn (RandomForest + GroupKFold) | 세션 단위로 완전히 분리한 교차검증 |

**작동 순서(파이프라인):**
1. **[아두이노]** A0(턱밑), A1(귀 아래) 두 핀을 1000Hz로 번갈아 읽어 `"ch0,ch1"` 형태로 시리얼 전송
2. **[파이썬] MVC 보정** — 세션 시작 시 "최대한 힘껏 근육에 힘주세요"를 3초간 시켜서, 그 활동량을 100% 기준(MVC)으로 저장
3. **[파이썬] 신호처리** — 원시 신호 → 대역통과 필터(20~450Hz) → 노치 필터(60Hz) → 정류(절댓값) → 포락선(저역통과) 순으로 처리
4. **[파이썬] 정규화** — 포락선 값을 MVC로 나누어, 사람마다·세션마다 다른 신호 크기를 보정
5. **[파이썬] 저장** — 라벨, 세션 번호와 함께 평균·RMS·최댓값을 `semg_features.csv`에 저장
6. **[파이썬] 검증** — `q` 입력 시, **세션 단위로 완전히 분리한** 교차검증(GroupKFold)으로 baseline 정확도를 계산

**왜 이렇게 만들었나 (설계 근거):**
- **대역통과+노치 필터**: sEMG는 신호 자체가 미세(mV 단위)해서 60Hz 전원 잡음과 저주파 움직임 잡음에 특히 취약합니다. 이 두 필터 없이는 신호보다 잡음이 클 수 있습니다.
- **MVC 정규화**: 5장에서 지적한 "전극 접촉 상태·개인차에 따라 신호 크기가 들쭉날쭉하다"는 문제를, 하드웨어(게인 조절 불가한 Gravity EMG 센서)가 아니라 소프트웨어 단에서 보정하기 위한 장치입니다.
- **세션 단위 GroupKFold**: 01/02 코드의 일반 K-fold는 같은 세션 데이터가 훈련·검증에 섞여 들어가 정확도가 실제보다 좋게 나오는 착시를 만들 수 있습니다. 이 코드는 "세션 3으로 검증할 때 세션 1·2 데이터는 아예 안 보이게" 분리해서, 8장에서 확인하려는 '세션 간 재현성'을 있는 그대로 측정합니다.
- **실측 샘플링 레이트 확인**: 아두이노 우노의 실제 처리 속도는 코드·시리얼 설정에 따라 달라질 수 있어, 이론값을 그대로 믿지 않고 매 녹화마다 직접 측정해 목표 대비 몇 %인지 출력합니다.

---

## 4. 실행 방법

### 4-1. 사전 준비 (Windows 기준)

1. Python 3.9 이상이 설치되어 있어야 합니다. (기존 `AI_study` 환경과
   동일한 가상환경을 쓰셔도 되고, 새로 만드셔도 됩니다.)
2. 각 폴더로 이동해서 필요한 패키지를 설치합니다.

```powershell
cd C:\AI_study\Deafness_AI\01_webcam_vsr
pip install -r requirements.txt

cd C:\AI_study\Deafness_AI\02_mic_acoustic
pip install -r requirements.txt
```

> `librosa`는 설치 시 `soundfile`, `numba` 등 부가 패키지를 함께 설치하며
> 다소 시간이 걸릴 수 있습니다. 인터넷 연결 상태에서 여유를 두고 진행하세요.

### 4-2. 웹캠 테스트 실행

```powershell
cd C:\AI_study\Deafness_AI\01_webcam_vsr
python lip_landmark_capture.py
```

- 실행하면 라벨 목록이 뜹니다: `m, n, ng, g, k, kk`
- 라벨(예: `m`)을 입력하고 Enter → 웹캠 창이 뜨고 1.5초간 촬영됩니다.
  그 사이 해당 음소(예: "음~음~음~")를 반복 발음하세요.
- 각 라벨을 **최소 5회 이상** 반복하시는 걸 추천합니다. (분류기 테스트가
  라벨당 5개 미만이면 생략됩니다.)
- 다 하셨으면 `q`를 입력해 종료 → 자동으로 baseline 정확도가 출력됩니다.
- 만약 웹캠이 안 열리면, 코드 안의 `cv2.VideoCapture(0)` 줄을
  `cv2.VideoCapture(0, cv2.CAP_DSHOW)`로 바꿔서 다시 시도해보세요.

### 4-3. 마이크 테스트 실행

```powershell
cd C:\AI_study\Deafness_AI\02_mic_acoustic
python audio_feature_capture.py
```

- 사용법은 웹캠 스크립트와 동일합니다 (라벨 입력 → 1.5초 녹음 → 반복 → `q`로 종료).
- 여러 마이크 장치가 있어서 인식이 잘 안 되면, 파이썬 콘솔에서
  아래 코드로 장치 목록을 확인하고 기본 장치를 바꿀 수 있습니다.

```python
import sounddevice as sd
print(sd.query_devices())
sd.default.device = 1  # 원하는 장치 번호로 변경
```

---

### 4-4. sEMG 테스트 실행

**1단계 — 아두이노 업로드**
1. Arduino IDE에서 `03_semg/arduino_emg_reader/arduino_emg_reader.ino` 열기
2. 보드/포트 선택 후 업로드
3. 시리얼 모니터를 열어 `"512,600"` 같은 숫자 쌍이 계속 출력되는지 확인하고, **확인 후에는 시리얼 모니터를 반드시 닫으세요** (파이썬이 포트를 못 열게 됩니다)

**2단계 — 파이썬 스크립트 준비**
```powershell
cd C:\AI_study\Deafness_AI\03_semg
pip install -r requirements.txt
```
`semg_feature_capture.py` 상단의 `SERIAL_PORT = "COM3"` 부분을 본인 환경의 실제 포트로 수정하세요. (장치관리자에서 확인 가능)

**3단계 — 실행**
```powershell
python semg_feature_capture.py
```
- 먼저 세션 번호를 입력합니다 (전극을 새로 붙일 때마다 번호를 바꿔서 여러 번 실행하세요 — 예: 오늘 1session, 다음날 2, 그 다음날 3)
- MVC 보정: 안내에 따라 3초간 최대한 힘껏 힘주기
- 이후 웹캠/마이크 스크립트와 동일하게 라벨 입력 → 녹화 → 반복 → `q`로 종료
- **최소 2세션 이상 실행해야** 진짜 의미 있는 검증(세션 독립적 baseline)이 나옵니다. 1세션만으로 실행하면 참고용 수치만 나오고 경고 메시지가 뜹니다.

---

## 5. 결과를 어떻게 해석하면 되는가

두 스크립트 모두 종료 시 다음과 같은 형식의 출력을 보여줍니다.

```
=== 웹캠(입모양) 단독 baseline 분류 정확도 ===
  3-fold 교차검증 평균 정확도: 63.2%  (각 fold: ['61.0%', '64.0%', '64.5%'])
  참고: 라벨 6개 기준 우연 수준(chance) 정확도 = 16.7%
```

- **우연 수준(chance) 대비 얼마나 높은가**가 핵심입니다. 라벨 6개면
  아무렇게나 찍어도 16.7% 정도는 맞으므로, 이보다 훨씬 높아야 의미가 있습니다.
- **비음(m, n, ng) vs 연구개음(g, k, kk) 그룹 간 정확도 차이**를 눈여겨보세요.
  보고서 가설대로라면, 웹캠은 비음보다 연구개음에서 더 헷갈려하고,
  반대로 마이크(음향)는 비음에서 상대적으로 더 잘 구분할 가능성이 있습니다.
- CSV 파일(`lip_features.csv`, `audio_features.csv`)은 엑셀로 열어서
  직접 라벨별 평균값 차이를 살펴보셔도 좋습니다.

---

## 6. 한계 (반드시 읽어주세요)

- 이 코드들은 **정식 논문 수준의 검증이 아니라, 빠른 감 잡기용 프로토타입**입니다.
  화자 1명(본인), 세션 1회, 매우 단순한 특징만 사용합니다.
- 웹캠 코드의 특징(너비/높이/비율 3개)은 정식 VSR 논문들이 쓰는 CNN
  기반 방식보다 훨씬 단순합니다. 여기서 정확도가 낮게 나온다고 해서
  "VSR 자체가 불가능하다"는 결론까지 내리기엔 이릅니다 — 다만 방향성
  (연구개음이 더 어려운가?) 정도는 참고할 수 있습니다.
- 마이크 코드의 `spectral_centroid`/`spectral_bandwidth`는 정식 포먼트
  분석이 아닌 근사치입니다.
- 조용한 환경에서, 카메라 정면을 보고, 마이크와 적당한 거리를 유지한
  상태에서 테스트해야 결과가 의미가 있습니다.
- sEMG 코드의 필터·MVC 정규화는 표준적인 기법을 적용한 것이지만,
  Gravity EMG 센서처럼 게인 조절이 안 되는 하드웨어의 한계(개인차·
  전극 상태에 따른 신호 크기 편차)를 완전히 없애주지는 못합니다.
  1채널만으로 연구개음을 판별하려는 시도는 5장에서 이미 검토했듯
  근본적인 한계가 있을 수 있다는 점을 다시 한번 감안해주세요.

---

## 7. 다음 단계

1. 세 채널(웹캠/마이크/sEMG) baseline을 각각 확보한 뒤, 세 CSV를 시간
   기준으로 합쳐서 "3채널 융합" 모델의 정확도를 baseline들과 비교합니다.
   (보고서 8장 파일럿 판정 기준과 연결)
2. sEMG는 특히 **최소 2세션 이상** 데이터를 모아 세션 독립적 검증까지
   마쳐야, 8장에서 정한 판정 기준(연구개음 세트 10~15%p 이상 개선)을
   실제로 확인할 수 있습니다.
3. 여기서 만든 단순 특징(기하학적 입모양, 근사 음향 특징, sEMG 포락선
   통계값)은 어디까지나 1단계 baseline이며, 본격 개발 단계에서는
   CNN/트랜스포머 기반 모델로 교체하는 것을 전제로 설계되었습니다.
