# WIKI — 04_audio_to_tongue (오디오 기반 혀 위치 추정)

> 이 문서는 기존 `WIKI.md`(01_webcam_vsr, 02_mic_acoustic, 03_semg)와는
> 별개로, sEMG 센서 없이 **마이크(오디오)만으로, 그리고 오디오+입술영상으로
> 혀 위치를 추정**하는 새로운 방향(04_audio_to_tongue)에 대한 문서입니다.

---

## 1. 이게 왜 필요한가 (배경)

기존 03_semg는 sEMG 센서를 붙여 혀 관련 근육 신호를 직접 측정하는 방식이었습니다.
그런데 다음 두 가지를 다시 검토하면서 방향을 하나 더 추가하게 되었습니다.

1. **대상 재검토**: 청각장애인 대상은 데이터·규제 진입장벽이 높아, 우선
   "한국인 대상 영어 발음 학습"으로 시작해보는 방안을 검토 중입니다.
   이 경우 사용자가 정상적으로 들을 수 있으므로, 굳이 센서를 붙이지 않고도
   마이크 피드백만으로 충분할 수 있습니다.
2. **데이터 가용성**: TaL Corpus(영어, 오디오+초음파 혀 영상+입술 영상
   페어 데이터)가 이미 공개되어 있어, "오디오만으로" 또는 "오디오+입모양으로"
   혀 위치를 추정하는 모델을 바로 학습해볼 수 있는 재료가 있습니다.

이 폴더의 코드는 **센서 없이, 오디오(+선택적으로 입술 영상) 입력만으로
혀 위치를 간접 추정**해서 화면에 시각 피드백(입모양 비교 + 혀 위치 참조
단면도)을 주기 위한 AI 모델을 만드는 전체 파이프라인입니다. 최신 버전에서는
**"오디오만" vs "오디오+입모양"** 두 버전을 나란히 학습·비교할 수 있습니다.

---

## 2. 폴더 구조

```
Deafness_AI/
└── 04_audio_to_tongue/
    ├── requirements.txt
    ├── 01_prepare_data.py          <- TaL 원시 데이터 전처리 (오디오+입술영상+혀위치)
    ├── 02_baseline_eigentongue.py  <- 1단계: 단순 회귀 베이스라인 (오디오만)
    ├── 03_train_model.py           <- 2단계: CNN 모델 학습 (오디오만)
    ├── 03b_train_model_av.py       <- 2단계: CNN 모델 학습 (오디오+입모양 융합)
    ├── 04_evaluate.py              <- 02 vs 03 vs 03b 3모델 화자독립 성능 비교
    ├── 05_realtime_inference.py    <- 실시간 마이크 데모 (오디오만 모델 기준)
    └── processed/                  <- 실행 후 자동 생성되는 결과물
        ├── train/, val/, test/     <- 화자별로 나뉜 전처리 완료 데이터(.npz, audio+video+tongue 포함)
        ├── eigentongue_pca.joblib  <- 학습된 PCA 모델
        ├── splits.json             <- 화자 분할 기록
        ├── baseline_ridge.joblib   <- 02단계 산출 모델
        ├── cnn_model_best.pt       <- 03단계 산출 모델 (오디오만)
        └── cnn_model_best_av.pt    <- 03b단계 산출 모델 (오디오+입모양)
```

---

## 3. 각 코드가 어떻게 작동하는가 (LLM 위키 스타일 설명)

### 3-1. `01_prepare_data.py` — 데이터 전처리 (오디오+입술영상+혀위치)

**한 줄 요약:** TaL 데이터의 오디오, 입술 영상(.mp4), 초음파 혀 영상을 각각 특징으로 바꾸고, 시간축을 모두 오디오 기준으로 맞춰서, 화자 단위로 학습/검증/테스트용으로 나눠 한 파일(.npz)에 함께 저장합니다.

**사용하는 기술:**
| 구성 요소 | 역할 |
|---|---|
| librosa | 오디오 → 멜 스펙트로그램(로그 스케일) 특징 추출 |
| OpenCV (`cv2`) | .mp4 입술 영상을 프레임 단위로 읽기 |
| MediaPipe FaceMesh | 입술 영상 프레임에서 입모양 랜드마크 추출 (01_webcam_vsr과 동일 방식) |
| numpy | 초음파 원시 바이너리(.ult) 파일을 프레임 배열로 읽기 |
| scikit-learn PCA | 초음파 프레임(수천 픽셀)을 3개의 숫자(eigentongue 계수)로 압축 |

**작동 순서:**
1. TaL 데이터 폴더를 순회하며 화자별로 발화 목록(오디오+입술영상+초음파 쌍)을 찾습니다. **`.mp4`가 없는 발화는 건너뜁니다** — "오디오만" 실험과 "오디오+입모양" 실험을 정확히 같은 발화 집합으로 공정 비교하기 위함입니다.
2. 초음파 프레임 일부를 샘플링해 **PCA(주성분분석)** 를 학습합니다. "이 화자 집단에서 혀 모양이 주로 어떤 방향으로 변하는가"의 상위 3개 패턴(주성분)만 남기고, 이게 조음 파라미터가 됩니다.
3. 각 발화에 대해 오디오 특징, 입모양 특징(너비/높이/비율 3개, 01_webcam_vsr과 동일), 혀 위치 파라미터를 각각 뽑습니다.
4. 오디오(초당 100프레임), 입술 영상(추정 초당 60프레임), 초음파(초당 약 80프레임)의 속도가 모두 달라서, **선형보간**으로 셋 다 오디오 프레임 수에 맞춰 리샘플링합니다.
5. 화자를 무작위로 train(대부분)/val(10%)/test(10%)로 나눠 저장합니다. **프레임을 섞지 않고 화자 단위로 통째로 나누는 게 핵심**입니다.

**입술 영상 처리 관련 세부사항:**
- 얼굴이 인식되지 않는 프레임은 직전에 인식됐던 값으로 채웁니다(forward-fill). 영상 맨 앞부분부터 인식이 안 되면 0으로 채웁니다 — 시계열 길이를 유지하기 위한 처리입니다.
- MediaPipe FaceMesh 인스턴스는 발화마다 새로 만들지 않고 재사용합니다(속도 최적화).

**왜 이렇게 만들었나 (설계 근거):**
- 초음파 원본 이미지를 그대로 예측 목표로 삼지 않고 PCA로 3차원까지 줄인 이유는, ①모델이 훨씬 가벼워지고 ②"정확한 이미지"가 아니라 "대략적인 방향"만 예측한다는 설계 철학과 맞아떨어지기 때문입니다.
- 입모양 특징을 01_webcam_vsr과 동일한 정의(너비/높이/비율)로 뽑은 이유는, 나중에 실제 배포 때 웹캠에서 뽑는 특징과 학습 때 쓰는 특징이 일치해야 모델이 실사용 환경에서도 통하기 때문입니다.

---

### 3-2. `02_baseline_eigentongue.py` — 1단계 베이스라인 (오디오만)

**한 줄 요약:** 오디오 프레임 하나 → 혀 위치 파라미터 하나를, 아주 단순한 선형회귀(Ridge)로 예측합니다. 앞뒤 문맥은 전혀 보지 않습니다.

**왜 이 단계가 먼저 필요한가:**
복잡한 딥러닝 모델을 만들기 전에 "오디오만으로 혀 위치를 어느 정도까지 맞힐 수 있는지"의 최소 기준선을 확인하기 위한 것입니다.

**결과 해석 방법:**
상관계수(r)와 R²를 화자독립 테스트셋에서 확인합니다. R²가 0에 가깝다는 건 "오디오 정보가 이 축을 예측하는 데 거의 도움이 안 된다"는 뜻입니다.

---

### 3-3. `03_train_model.py` — 2단계 CNN 모델 (오디오만)

**한 줄 요약:** 오디오의 앞뒤 흐름(시간적 문맥)을 함께 보는 경량 1D CNN으로, 베이스라인보다 나은 예측을 시도합니다.

**사용하는 기술:**
| 구성 요소 | 역할 |
|---|---|
| PyTorch | 신경망 정의·학습 |
| 1D Convolution | 오디오 특징의 시간축을 따라 패턴(문맥)을 학습 |
| Masked MSE | 발화 길이가 제각각이라 패딩한 부분은 손실 계산에서 제외 |

**작동 순서:**
1. 각 발화를 약 2초 단위(CHUNK_LEN=200 프레임)로 잘라 학습 샘플을 만듭니다.
2. 배치 안에서 길이가 다른 조각들은 짧은 쪽에 0을 채우고(padding), 실제 데이터가 있는 부분만 손실 계산에 반영합니다(masking).
3. Conv1D 3개 층을 거쳐 각 시점의 혀 위치 파라미터를 예측합니다.
4. 매 에폭마다 검증 손실을 확인해서, 가장 좋았던 모델만 저장합니다.

**왜 처음부터 크게 안 만들었나:**
데이터 규모가 크지 않을 수 있어 무거운 모델(Transformer 등)은 과적합 위험이 큽니다. 03_semg에서도 강조했던 "작은 데이터로 버티는" 원칙을 여기서도 그대로 적용해, 작은 CNN부터 시작합니다.

---

### 3-4. `03b_train_model_av.py` — 2단계 CNN 모델 (오디오+입모양 융합)

**한 줄 요약:** 03번과 완전히 같은 모델 구조·학습 설정을 쓰되, 입력에 오디오 특징과 입모양 특징(01_webcam_vsr과 동일한 3개 값)을 이어붙여(concat) 넣습니다.

**왜 03번과 구조를 완전히 똑같이 맞췄는가:**
"입모양을 추가해서 좋아졌다"는 결론이 **모델 구조 차이가 아니라 정보 자체의 차이 때문**이라고 말할 수 있으려면, 비교 대상 두 모델이 입력 채널 수(오디오만 n_mels개 vs 오디오+입모양 n_mels+3개) 외에는 완전히 동일해야 합니다. 그래서 Conv1D 층 수, hidden 크기, 학습률, 에폭 수, 배치 크기를 03번과 하나도 다르지 않게 맞췄습니다.

**작동 순서:**
1. `.npz`에서 `video` 키가 있는 데이터만 사용합니다(01_prepare_data.py의 최신 버전으로 만든 데이터).
2. 오디오 특징과 입모양 특징을 시간축은 그대로 두고 특징 차원(열 방향)으로 이어붙입니다.
3. 이후 흐름은 03번과 동일합니다.

---

### 3-5. `04_evaluate.py` — 3모델(Ridge / 오디오CNN / 오디오+입모양CNN) 비교

**한 줄 요약:** 02, 03, 03b 세 모델을 **완전히 같은, 학습에 한 번도 등장하지 않은 화자(test)** 에서 나란히 평가하고, "입모양을 추가하면 실제로 좋아지는가"를 자동으로 판정해 알려줍니다.

**결과를 어떻게 읽을지:**
- 평균 개선폭이 **+0.03보다 크면** → 입모양이 실질적으로 도움이 됨 → 실제 배포 때도 웹캠+마이크를 함께 04번 모델에 넣는 구조를 검토
- 평균 개선폭이 **-0.03보다 작으면** → 오히려 나빠짐 → 데이터 부족으로 인한 과적합 가능성 → 오디오 단독 구조 유지가 안전
- 그 사이(거의 동일)면 → 지금 데이터 규모에서는 판단하기 이르다는 뜻 → 더 큰 데이터(TaL80 전체 등)로 재검증 권장

**왜 이 판정 기준(±0.03)을 뒀는가:**
상관계수 r은 데이터가 적을 때 우연에 의한 변동(noise)만으로도 ±0.02~0.03 정도는 흔들릴 수 있습니다. 그보다 뚜렷한 차이가 나야 "진짜 효과"로 볼 만하다는 보수적인 기준입니다. 다만 이건 절대적 기준이 아니므로, 데이터가 충분히 커지면 통계적 유의성 검정(예: 부트스트랩 신뢰구간)으로 더 엄밀하게 확인하는 것을 권장합니다.

---

### 3-6. `05_realtime_inference.py` — 실시간 데모 (오디오만 모델 기준)

**한 줄 요약:** 마이크로 실시간 음성을 받아, 0.5초마다 03번 모델(오디오만)로 혀 위치를 추정하고 콘솔에 막대 그래프로 보여줍니다.

**참고:** 현재는 03번(오디오만) 모델 기준으로만 작성되어 있습니다. 04번 비교에서 "오디오+입모양"이 뚜렷이 더 낫다는 결과가 나오면, 이 스크립트도 웹캠 프레임을 함께 읽어 03b 모델(`cnn_model_best_av.pt`)을 쓰도록 확장하는 것을 고려하세요.

---

## 4. 실행 방법

### 4-1. 사전 준비

```powershell
cd C:\AI_study\Deafness_AI\04_audio_to_tongue
pip install -r requirements.txt
```

> `mediapipe==0.10.14`로 버전을 고정해뒀습니다. 최신 버전 중 일부는 pip 배포판에
> `mp.solutions`(레거시 FaceMesh API)가 빠져 있는 경우가 있어, 이미 검증된
> 버전으로 고정했습니다. Windows 환경에서 최신 버전이 정상 동작한다면 버전
> 제한을 풀어도 됩니다.

TaL 데이터를 다운로드해 압축을 해제한 뒤, 그 경로를 환경변수로 지정합니다.

```powershell
set TAL_ROOT=D:\datasets\TaL80
```

### 4-2. 순서대로 실행

```powershell
python 01_prepare_data.py
python 02_baseline_eigentongue.py
python 03_train_model.py
python 03b_train_model_av.py
python 04_evaluate.py
python 05_realtime_inference.py
```

각 단계는 이전 단계의 결과물(`processed/` 폴더 안 파일)을 그대로 사용하므로, 반드시 이 순서를 지켜야 합니다. **03b는 03과 독립적으로 실행 가능**하지만(둘 다 01의 결과물만 있으면 됨), 04에서 비교하려면 둘 다 먼저 실행해야 합니다.

### 4-3. 처음 실행 시 꼭 확인할 것

- TaL 데이터를 실제로 받은 뒤, 화자 1~2명만으로 `01_prepare_data.py`를 먼저 돌려보세요. `.ult`/`.param` 파일의 정확한 바이너리 구조나 폴더 규칙이 배포본과 다르면 이 단계에서 오류가 나거나 프레임 수가 이상하게 나올 수 있습니다.
- **`.mp4` 입술 영상의 실제 프레임레이트를 확인하세요.** 코드에는 60fps로 가정해뒀는데(`VIDEO_FPS_DEFAULT`), 실제 파일 메타데이터와 다르면 `cv2.VideoCapture(path).get(cv2.CAP_PROP_FPS)`로 직접 확인 후 값을 수정하세요.
- 처리 결과 로그의 "건너뛴 발화 사유"에 `no_video`나 `video_read_fail`이 많이 뜨면, mp4 파일 존재 여부나 코덱 문제를 먼저 점검하세요.

---

## 5. 결과를 어떻게 해석하면 되는가

`04_evaluate.py`를 실행하면 이런 형태의 출력이 나옵니다.

```
=== 03. CNN (오디오만, 문맥 포함) — 화자독립 테스트셋 최종 성능 ===
  조음 파라미터 1번 축: r=0.412, RMSE=12.3

=== 03b. CNN (오디오+입모양, 문맥 포함) — 화자독립 테스트셋 최종 성능 ===
  조음 파라미터 1번 축: r=0.501, RMSE=10.8

=== 종합 비교: 입모양 추가 효과 ===
  조음 파라미터 1번 축: 오디오만 r=0.412 → 오디오+입모양 r=0.501  (▲ 개선, 차이 +0.089)
  ...
  평균 개선폭: +0.075
  -> 입모양 정보가 혀 위치 예측에 실질적으로 도움이 됩니다.
```

- **r(상관계수)이 핵심 지표**입니다. 1에 가까울수록 예측이 실제 혀 위치 변화와 잘 맞는다는 뜻입니다.
- 음성학적으로 오디오-조음 역변환은 원래 다대일 매핑 문제라 r=1.0에 가까운 완벽한 예측은 기대하기 어렵습니다. r이 0.3~0.5 정도만 나와도 "방향성 힌트"로는 쓸모가 있을 수 있습니다.
- 축마다, 그리고 오디오만/오디오+입모양 사이에 개선폭이 다를 수 있습니다. **어떤 축(예: 입술과 관련된 순음 계열)은 입모양 추가로 크게 좋아지고, 어떤 축(예: 혀 뒤쪽과 관련된 축)은 거의 그대로일 수 있습니다** — 4~5장(원 보고서)에서 논의했던 "입모양은 순음에는 강하지만 후방 조음에는 약하다"는 가설과 같은 맥락으로 해석할 수 있습니다.

---

## 6. 한계 (반드시 읽어주세요)

- **오디오만으로 혀 위치를 완벽히 알아내는 건 원리적으로 불가능합니다.** 서로 다른 혀 모양이 비슷한 소리를 낼 수 있기 때문입니다(다대일 매핑).
- **TaL 데이터는 영어 원어민 데이터입니다.** 한국인 화자, 특히 한국인이 발음하는 영어에 이 모델이 그대로 잘 통할지는 검증되지 않았습니다.
- **.ult/.param, 입술 영상 fps 모두 논문·문서 기반의 추정입니다.** 실제 배포 데이터와 다를 수 있으니, 처음 실행 시 소량으로 반드시 검증하세요.
- **PCA 기반 조음 파라미터는 해석이 명확하지 않을 수 있습니다.** 학습 후 실제 초음파 영상과 비교하며 각 축이 무엇을 의미하는지 별도로 확인하는 작업이 필요합니다.
- **입모양 특징(01_webcam_vsr 방식)도 단순화된 기하학적 값(너비/높이/비율) 3개뿐입니다.** 정식 CNN 기반 VSR보다 정보량이 훨씬 적어서, "입모양 추가 효과가 없다"는 결과가 나와도 "입모양 자체가 무의미하다"가 아니라 "이 정도로 단순화한 입모양 특징으로는 효과가 안 보인다"로 해석하는 것이 정확합니다.
- CNN 모델은 청각장애인 대상 03_semg처럼 세션 간 재현성 문제는 다루지 않았습니다.

---

## 7. 다음 단계

1. **04번 비교 결과부터 확인**합니다 — 입모양 추가가 실질적으로 도움이 되는지가 이후 방향을 정하는 분기점입니다.
2. 도움이 된다면, `05_realtime_inference.py`를 웹캠+마이크를 함께 읽어 03b 모델을 쓰도록 확장합니다.
3. 도움이 안 된다면, 오디오 단독(03번) 구조를 유지하되, 더 큰 데이터(TaL80 전체)로 한 번 더 검증해 결론을 확정합니다.
4. 화자독립 r이 실사용에 쓸 만한 수준인지 판단한 뒤, 01_webcam_vsr과의 화면 통합 데모로 넘어갑니다.
5. 한국인이 실제로 발음한 영어 음성(+입모양)으로 소규모 검증을 진행해, TaL(영어 원어민) 데이터로 학습한 모델이 한국인 화자에게도 통하는지 확인합니다.
6. 이 모델이 "청각장애인 자가 조음훈련"으로 되돌아갈 경우에도 재사용 가능한지 함께 검토합니다.
