# WIKI — 04_audio_to_tongue (오디오 기반 혀 위치 추정)

> 이 문서는 기존 `WIKI.md`(01_webcam_vsr, 02_mic_acoustic, 03_semg)와는
> 별개로, sEMG 센서 없이 **마이크(오디오)만으로 혀 위치를 추정**하는
> 새로운 방향(04_audio_to_tongue)에 대한 문서입니다.

---

## 1. 이게 왜 필요한가 (배경)

기존 03_semg는 sEMG 센서를 붙여 혀 관련 근육 신호를 직접 측정하는 방식이었습니다.
그런데 다음 두 가지를 다시 검토하면서 방향을 하나 더 추가하게 되었습니다.

1. **대상 재검토**: 청각장애인 대상은 데이터·규제 진입장벽이 높아, 우선
   "한국인 대상 영어 발음 학습"으로 시작해보는 방안을 검토 중입니다.
   이 경우 사용자가 정상적으로 들을 수 있으므로, 굳이 센서를 붙이지 않고도
   마이크 피드백만으로 충분할 수 있습니다.
2. **데이터 가용성**: TaL Corpus(영어, 오디오+초음파 혀 영상 페어 데이터)가
   이미 공개되어 있어, "오디오만으로 혀 위치를 추정하는 모델"을 바로
   학습해볼 수 있는 재료가 있습니다.

이 폴더의 코드는 **센서 없이, 오디오 입력만으로 혀 위치를 간접 추정**해서
화면에 시각 피드백(입모양 비교 + 혀 위치 참조 단면도)을 주기 위한
AI 모델을 만드는 전체 파이프라인입니다.

---

## 2. 폴더 구조

```
Deafness_AI/
└── 04_audio_to_tongue/
    ├── requirements.txt
    ├── 01_prepare_data.py          <- TaL 원시 데이터 전처리
    ├── 02_baseline_eigentongue.py  <- 1단계: 단순 회귀 베이스라인
    ├── 03_train_model.py           <- 2단계: CNN 기반 모델 학습
    ├── 04_evaluate.py              <- 02 vs 03 화자독립 성능 비교
    ├── 05_realtime_inference.py    <- 실시간 마이크 데모
    └── processed/                  <- 실행 후 자동 생성되는 결과물
        ├── train/, val/, test/     <- 화자별로 나뉜 전처리 완료 데이터(.npz)
        ├── eigentongue_pca.joblib  <- 학습된 PCA 모델
        ├── splits.json             <- 화자 분할 기록
        ├── baseline_ridge.joblib   <- 02단계 산출 모델
        └── cnn_model_best.pt       <- 03단계 산출 모델
```

---

## 3. 각 코드가 어떻게 작동하는가 (LLM 위키 스타일 설명)

### 3-1. `01_prepare_data.py` — 데이터 전처리

**한 줄 요약:** TaL 데이터의 오디오와 초음파 혀 영상을 각각 특징으로 바꾸고, 시간축을 맞춰서, 화자 단위로 학습/검증/테스트용으로 나눠 저장합니다.

**사용하는 기술:**
| 구성 요소 | 역할 |
|---|---|
| librosa | 오디오 → 멜 스펙트로그램(로그 스케일) 특징 추출 |
| numpy | 초음파 원시 바이너리(.ult) 파일을 프레임 배열로 읽기 |
| scikit-learn PCA | 초음파 프레임(수천 픽셀)을 3개의 숫자(eigentongue 계수)로 압축 |

**작동 순서:**
1. TaL 데이터 폴더를 순회하며 화자별로 발화 목록(오디오+초음파 쌍)을 찾습니다.
2. 그중 일부를 샘플링해 **PCA(주성분분석)** 를 학습합니다. 초음파 이미지 하나하나를 그대로 쓰지 않고, "이 화자 집단에서 혀 모양이 주로 어떤 방향으로 변하는가"의 상위 3개 패턴(주성분)만 남깁니다. 이게 곧 조음 파라미터가 됩니다.
3. 각 발화에 대해 오디오 특징과 (PCA로 압축된) 혀 위치 파라미터를 각각 뽑습니다.
4. 오디오는 초당 100프레임, 초음파는 초당 약 80프레임으로 속도가 달라서, **선형보간**으로 오디오 프레임 수에 맞춰 혀 파라미터를 리샘플링합니다.
5. 화자를 무작위로 train(대부분)/val(10%)/test(10%)로 나눠 저장합니다. **이때 프레임을 섞지 않고 화자 단위로 통째로 나누는 게 핵심**입니다 — 같은 화자의 다른 발화가 학습과 테스트에 동시에 들어가면, 실제로는 "새로운 사람에게도 통하는지"를 검증하지 못하고 결과가 과장됩니다.

**왜 이렇게 만들었나 (설계 근거):**
초음파 원본 이미지를 그대로 예측 목표로 삼지 않고 PCA로 3차원까지 줄인 이유는, ①모델이 훨씬 가벼워지고 ②"정확한 이미지"가 아니라 "대략적인 방향"만 예측한다는 설계 철학(화면에 점선/개략적으로 표시하기로 한 원칙)과 맞아떨어지기 때문입니다.

---

### 3-2. `02_baseline_eigentongue.py` — 1단계 베이스라인

**한 줄 요약:** 오디오 프레임 하나 → 혀 위치 파라미터 하나를, 아주 단순한 선형회귀(Ridge)로 예측합니다. 앞뒤 문맥은 전혀 보지 않습니다.

**왜 이 단계가 먼저 필요한가:**
복잡한 딥러닝 모델을 만들기 전에 "오디오만으로 혀 위치를 어느 정도까지 맞힐 수 있는지"의 최소 기준선을 확인하기 위한 것입니다. 이후 만드는 CNN 모델이 이 베이스라인보다 못하다면, 애초에 모델 설계가 잘못됐다는 신호입니다.

**결과 해석 방법:**
상관계수(r)와 R²를 화자독립 테스트셋에서 확인합니다. R²가 0에 가깝다는 건 "오디오 정보가 이 축을 예측하는 데 거의 도움이 안 된다"는 뜻입니다.

---

### 3-3. `03_train_model.py` — 2단계 CNN 모델

**한 줄 요약:** 오디오의 앞뒤 흐름(시간적 문맥)을 함께 보는 경량 1D CNN으로, 베이스라인보다 나은 예측을 시도합니다.

**사용하는 기술:**
| 구성 요소 | 역할 |
|---|---|
| PyTorch | 신경망 정의·학습 |
| 1D Convolution | 오디오 특징의 시간축을 따라 패턴(문맥)을 학습 |
| Masked MSE | 발화 길이가 제각각이라 패딩한 부분은 손실 계산에서 제외 |

**작동 순서:**
1. 각 발화를 약 2초 단위(CHUNK_LEN=200 프레임)로 잘라 학습 샘플을 만듭니다.
2. 배치 안에서 길이가 다른 조각들은 짧은 쪽에 0을 채우고(padding), 실제 데이터가 있는 부분만 손실 계산에 반영합니다(masking).
3. Conv1D 3개 층을 거쳐 각 시점의 혀 위치 파라미터를 예측합니다.
4. 매 에폭마다 검증 손실을 확인해서, 가장 좋았던 모델만 저장합니다.

**왜 처음부터 크게 안 만들었나:**
데이터 규모가 크지 않을 수 있어 무거운 모델(Transformer 등)은 과적합 위험이 큽니다. 03_semg에서도 강조했던 "작은 데이터로 버티는" 원칙을 여기서도 그대로 적용해, 작은 CNN부터 시작합니다.

---

### 3-4. `04_evaluate.py` — 베이스라인 vs CNN 비교

**한 줄 요약:** 02번과 03번 모델을 **완전히 같은, 학습에 한 번도 등장하지 않은 화자(test)** 에서 나란히 평가해서, CNN이 실제로 더 나은지 확인합니다.

**결과를 어떻게 읽을지:**
- CNN의 r이 Ridge보다 뚜렷이 높다 → 시간적 문맥이 실제로 도움이 된다는 뜻 → 2단계 모델 채택
- 둘 다 r이 낮다 → 오디오만으로 혀 위치 추정 자체가 어렵다는 뜻 → 화면에서 "개략적" 표시를 더 강하게 하거나, 이 방향 자체를 재검토해야 함

---

### 3-5. `05_realtime_inference.py` — 실시간 데모

**한 줄 요약:** 마이크로 실시간 음성을 받아, 0.5초마다 학습된 CNN 모델로 혀 위치를 추정하고 콘솔에 막대 그래프로 보여줍니다.

**작동 순서:**
1. 마이크 스트림을 1초 분량 버퍼에 계속 채워 넣습니다.
2. 0.5초마다 버퍼 전체로 멜 스펙트로그램을 뽑고 모델에 넣습니다.
3. 가장 최근 몇 프레임의 예측값 평균을 콘솔에 텍스트 막대(`[----●----]`)로 표시합니다.

**참고:** 이건 어디까지나 "모델이 실시간으로 잘 도는지" 확인하는 콘솔 버전입니다. 실제 앱에서는 이 예측값을 이전에 설계한 화면 UI(정적 참조 단면도 + 점선 마커)에 연결하면 됩니다.

---

## 4. 실행 방법

### 4-1. 사전 준비

```powershell
cd C:\AI_study\Deafness_AI\04_audio_to_tongue
pip install -r requirements.txt
```

TaL 데이터를 다운로드해 압축을 해제한 뒤, 그 경로를 환경변수로 지정합니다.

```powershell
set TAL_ROOT=D:\datasets\TaL80
```

### 4-2. 순서대로 실행

```powershell
python 01_prepare_data.py
python 02_baseline_eigentongue.py
python 03_train_model.py
python 04_evaluate.py
python 05_realtime_inference.py
```

각 단계는 이전 단계의 결과물(`processed/` 폴더 안 파일)을 그대로 사용하므로, 반드시 이 순서를 지켜야 합니다.

### 4-3. 처음 실행 시 꼭 확인할 것

TaL 데이터를 실제로 받은 뒤, 화자 1~2명만으로 `01_prepare_data.py`를 먼저 돌려보세요. `.ult`/`.param` 파일의 정확한 바이너리 구조나 폴더 규칙이 배포본과 다르면 이 단계에서 오류가 나거나 프레임 수가 이상하게 나올 수 있습니다. 그 경우 `find_utterances()`, `read_ultrasound_raw()` 함수의 가정 부분만 실제 데이터에 맞게 수정하면 됩니다.

---

## 5. 결과를 어떻게 해석하면 되는가

`04_evaluate.py`를 실행하면 이런 형태의 출력이 나옵니다.

```
=== 03. CNN 모델 (문맥 포함) — 화자독립 테스트셋 최종 성능 ===
  조음 파라미터 1번 축: r=0.412, RMSE=12.3
  조음 파라미터 2번 축: r=0.298, RMSE=15.1
```

- **r(상관계수)이 이 결과 해석의 핵심 지표**입니다. 1에 가까울수록 예측이 실제 혀 위치 변화와 잘 맞는다는 뜻이고, 0에 가까우면 거의 무관하다는 뜻입니다.
- 음성학적으로 오디오-조음 역변환(acoustic-to-articulatory inversion)은 원래 다대일 매핑 문제라 r=1.0에 가까운 완벽한 예측은 기대하기 어렵습니다. **r이 0.3~0.5 정도만 나와도 "방향성 힌트"로는 쓸모가 있을 수 있습니다** — 화면에서 정밀 좌표가 아니라 "대략 앞/뒤" 정도만 보여줄 것이기 때문입니다.
- 축마다 정확도가 다를 수 있습니다. 어떤 축은 잘 맞고 어떤 축은 안 맞을 수 있는데, 이 경우 화면 UI에서 정확도가 낮은 축은 아예 빼거나 더 흐릿하게 표시하는 것도 방법입니다.

---

## 6. 한계 (반드시 읽어주세요)

- **오디오만으로 혀 위치를 완벽히 알아내는 건 원리적으로 불가능합니다.** 서로 다른 혀 모양이 비슷한 소리를 낼 수 있기 때문입니다(다대일 매핑). 이 코드는 "완벽한 재구성"이 아니라 "참고할 만한 방향성 힌트"를 목표로 설계되었습니다.
- **TaL 데이터는 영어 원어민 데이터입니다.** 한국인 화자, 특히 한국인이 발음하는 영어에 이 모델이 그대로 잘 통할지는 검증되지 않았습니다. 실제 사용자 대상 검증이 반드시 필요합니다.
- **.ult/.param 파싱 코드는 논문에 기술된 스펙 기반의 추정입니다.** 실제 배포 데이터와 다를 수 있으니, 처음 실행 시 소량으로 반드시 검증하세요.
- **PCA 기반 조음 파라미터는 해석이 명확하지 않을 수 있습니다.** "1번 축이 혀의 전후 위치"라고 보기 좋게 나올 수도 있지만, 실제로는 여러 움직임이 섞인 축일 수 있습니다. 학습 후 실제 초음파 영상과 비교하며 각 축이 무엇을 의미하는지 별도로 확인하는 작업이 필요합니다.
- CNN 모델은 청각장애인 대상 03_semg처럼 세션 간 재현성 문제는 다루지 않았습니다(오디오는 sEMG만큼 전극 재부착 이슈가 없어 상대적으로 덜 중요하지만, 마이크 종류·환경 소음에 따른 변동성은 별도로 검증이 필요합니다).

---

## 7. 다음 단계

1. **화자독립 r이 실사용에 쓸 만한 수준인지 먼저 판단**합니다 (4장 참고). 너무 낮으면 이 방향 자체를 재검토해야 합니다.
2. r이 쓸 만하면, 01_webcam_vsr(입모양)과 이 모델(혀 위치)을 하나의 화면에 결합하는 통합 데모를 만듭니다.
3. 한국인이 실제로 발음한 영어 음성으로 소규모 검증을 진행해, TaL(영어 원어민) 데이터로 학습한 모델이 한국인 화자에게도 통하는지 확인합니다.
4. 이 모델이 "청각장애인 자가 조음훈련"으로 되돌아갈 경우에도 재사용 가능한지(즉 sEMG 없이 마이크만으로 혀 위치 힌트를 주는 옵션으로 편입 가능한지)도 함께 검토합니다.
